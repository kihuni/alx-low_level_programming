#include "main2.h"

#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always EXIT_SUCCESS.
 */

int main()
{
    int a = 12, b = 13;
    add(a,b);
    sub(a,b);
    mul(a,b);
    div(a,b);
    mod(a,b);


    return (0);
}